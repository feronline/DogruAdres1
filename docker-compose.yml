version: '3.8'

services:
  dogruadres:
    build: .
    ports:
      - "4949:4949"
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ASPNETCORE_URLS=http://0.0.0.0:4949
    restart: unless-stopped
    container_name: dogruadres-app
